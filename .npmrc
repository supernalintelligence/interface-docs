# GitHub Package Registry for @supernalintelligence scope
@supernalintelligence:registry=https://npm.pkg.github.com

# Auth: Local dev uses token from ~/.npmrc
# CI: Set NPM_TOKEN env var before npm install
