{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "echo '//npm.pkg.github.com/:_authToken=${NPM_TOKEN}' >> .npmrc && cp package.vercel.json package.json && npm install",
  "env": {
    "NODE_ENV": "production",
    "NEXT_PUBLIC_ENABLE_ANALYTICS": "true",
    "NEXT_PUBLIC_CONTACT_EMAIL": "hello@supernal.ai",
    "NEXT_PUBLIC_GLASS_MODE": "true"
  }
}
