/**
 * Shared test configuration
 * This file is used by both the dev server and Playwright tests
 * to ensure they use the same port
 */

module.exports = {
  // Default port for the dev server
  // This can be overridden if the port is already in use
  DEFAULT_PORT: 3011,
  
  // Base URL for tests
  getBaseURL: (port = 3011) => `http://localhost:${port}`,
};

