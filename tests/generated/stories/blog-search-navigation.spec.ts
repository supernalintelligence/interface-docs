/**
 * ⚠️  AUTO-GENERATED FILE - DO NOT EDIT MANUALLY!
 * 
 * ❌ WARNING: ANY MANUAL EDITS WILL BE OVERWRITTEN ON NEXT GENERATION
 * 
 * This file is automatically generated from Gherkin feature files.
 * To make changes:
 *   1. Edit the .feature file in features/
 *   2. Regenerate: npx si generate-story-tests features -o tests/generated/stories
 * 
 * Generated Playwright Test
 * Feature: Blog Search Navigation
 * 
 * Generated by: GraphTestGenerator
 * Generated at: 2026-01-31T23:37:41.076Z
 */

import { test, expect } from '@playwright/test';
import { testId } from '@supernal/interface/testing';
import { Blog, Chat } from '../../../src/architecture/ComponentNames';
import { initializeTestState } from '@supernal/interface/testing';

test.describe('Blog Search Navigation', () => {
  test.setTimeout(30000);

  // Initialize test state manager after page navigation
  test.afterEach(async ({ page }) => {
    // Clean up test state after each test
  });

  test('Navigate to blog index without query', async ({ page }) => {
    // Execution order: Navigate to blog index without query-415 → Navigate to blog index without query-416 → Navigate to blog index without query-417 → Navigate to blog index without query-418 → Navigate to blog index without query-419 → Navigate to blog index without query-420 → Navigate to blog index without query-421 → Navigate to blog index without query-422
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // When I type "open blog" in Components.Chat.input
    await page.locator(testId(Chat.input)).fill('open blog');

    // And I click Components.Chat.sendButton
    await page.locator(testId(Chat.sendButton)).first().click();

    // Then Components.Chat.messages should contain a new message
    await expect(page.locator(testId(Chat.messages))).not.toBeEmpty();

    // And the last message should contain "Navigating to Blog"
    // TODO: Use component reference for message container (e.g., Components.Chat.messages)
    await expect(page.locator('[data-testid*="messages"]').locator('> *:last-child')).toContainText('Navigating to Blog');

    // And the current URL should be "/blog"
    await expect(page).toHaveURL('/blog');

  });

  test('Search for specific blog post with full query', async ({ page }) => {
    // Execution order: Search for specific blog post with full query-423 → Search for specific blog post with full query-424 → Search for specific blog post with full query-425 → Search for specific blog post with full query-426 → Search for specific blog post with full query-427 → Search for specific blog post with full query-428 → Search for specific blog post with full query-429 → Search for specific blog post with full query-430 → Search for specific blog post with full query-431 → Search for specific blog post with full query-432
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // Given there is a blog post titled "Your Users Want It To Work"
    // TODO: Implement Given step: there is a blog post titled "Your Users Want It To Work"

    // When I type "open your users" in Components.Chat.input
    await page.locator(testId(Chat.input)).fill('open your users');

    // And I click Components.Chat.sendButton
    await page.locator(testId(Chat.sendButton)).first().click();

    // Then Components.Chat.messages should contain a new message
    await expect(page.locator(testId(Chat.messages))).not.toBeEmpty();

    // And the last message should start with "✅"
    // TODO: Use component reference for message container (e.g., Components.Chat.messages)
    await expect(page.locator('[data-testid*="messages"]').locator('> *:last-child')).toContainText(/^✅/);

    // And the last message should contain "Opening: Your Users Want It To Work"
    // TODO: Use component reference for message container (e.g., Components.Chat.messages)
    await expect(page.locator('[data-testid*="messages"]').locator('> *:last-child')).toContainText('Opening: Your Users Want It To Work');

    // And the current URL should match "/blog/your-users"
    await expect(page).toHaveURL(/\/blog\/your-users/);

  });

  test('Search with partial match', async ({ page }) => {
    // Execution order: Search with partial match-433 → Search with partial match-434 → Search with partial match-435 → Search with partial match-436 → Search with partial match-437 → Search with partial match-438 → Search with partial match-439 → Search with partial match-440 → Search with partial match-441
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // Given there is a blog post about "Agentic UX"
    // TODO: Implement Given step: there is a blog post about "Agentic UX"

    // When I type "open agentic ux" in Components.Chat.input
    await page.locator(testId(Chat.input)).fill('open agentic ux');

    // And I click Components.Chat.sendButton
    await page.locator(testId(Chat.sendButton)).first().click();

    // Then Components.Chat.messages should contain a new message
    await expect(page.locator(testId(Chat.messages))).not.toBeEmpty();

    // And the last message should start with "✅"
    // TODO: Use component reference for message container (e.g., Components.Chat.messages)
    await expect(page.locator('[data-testid*="messages"]').locator('> *:last-child')).toContainText(/^✅/);

    // And the current URL should contain "/blog/"
    await expect(page.url()).toContain('/blog/');

  });

  test('Pattern matching with placeholders', async ({ page }) => {
    // Execution order: Pattern matching with placeholders-442 → Pattern matching with placeholders-443 → Pattern matching with placeholders-444 → Pattern matching with placeholders-445 → Pattern matching with placeholders-446 → Pattern matching with placeholders-447 → Pattern matching with placeholders-448
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // When I type "open contracts" in Components.Chat.input
    await page.locator(testId(Chat.input)).fill('open contracts');

    // And I click Components.Chat.sendButton
    await page.locator(testId(Chat.sendButton)).first().click();

    // Then Components.Chat.messages should contain a new message
    await expect(page.locator(testId(Chat.messages))).not.toBeEmpty();

    // And the last message should not contain "❌"
    // TODO: Use component reference for message container (e.g., Components.Chat.messages)
    await expect(page.locator('[data-testid*="messages"]').locator('> *:last-child')).not.toContainText('❌');

  });

  test('Scoped tool prioritization over global navigation', async ({ page }) => {
    // Execution order: Scoped tool prioritization over global navigation-449 → Scoped tool prioritization over global navigation-450 → Scoped tool prioritization over global navigation-451 → Scoped tool prioritization over global navigation-452 → Scoped tool prioritization over global navigation-453 → Scoped tool prioritization over global navigation-454 → Scoped tool prioritization over global navigation-455 → Scoped tool prioritization over global navigation-456 → Scoped tool prioritization over global navigation-457
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // When I type "open your users" in Components.Chat.input
    await page.locator(testId(Chat.input)).fill('open your users');

    // And I click Components.Chat.sendButton
    await page.locator(testId(Chat.sendButton)).first().click();

    // Then Components.Chat.messages should contain a new message
    await expect(page.locator(testId(Chat.messages))).not.toBeEmpty();

    // And the last message should contain "Opening:"
    // TODO: Use component reference for message container (e.g., Components.Chat.messages)
    await expect(page.locator('[data-testid*="messages"]').locator('> *:last-child')).toContainText('Opening:');

    // And the last message should not contain "Navigate to Stories"
    // TODO: Use component reference for message container (e.g., Components.Chat.messages)
    await expect(page.locator('[data-testid*="messages"]').locator('> *:last-child')).not.toContainText('Navigate to Stories');

    // And the current URL should contain "/blog/"
    await expect(page.url()).toContain('/blog/');

  });

  test('UI feedback on successful search', async ({ page }) => {
    // Execution order: UI feedback on successful search-458 → UI feedback on successful search-459 → UI feedback on successful search-460 → UI feedback on successful search-461 → UI feedback on successful search-462 → UI feedback on successful search-463 → UI feedback on successful search-464 → UI feedback on successful search-465
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // When I type "open blog post" in Components.Chat.input
    await page.locator(testId(Chat.input)).fill('open blog post');

    // And I click Components.Chat.sendButton
    await page.locator(testId(Chat.sendButton)).first().click();

    // Then Components.Chat.messages should contain a new message
    await expect(page.locator(testId(Chat.messages))).not.toBeEmpty();

    // And the last message should start with "✅"
    // TODO: Use component reference for message container (e.g., Components.Chat.messages)
    await expect(page.locator('[data-testid*="messages"]').locator('> *:last-child')).toContainText(/^✅/);

    // And the last message should be visible
    await page.locator('[data-testid="unknown-selector-needs-manual-fix"]').first().waitFor({ state: 'visible' });

  });

  test('UI feedback on failed search', async ({ page }) => {
    // Execution order: UI feedback on failed search-466 → UI feedback on failed search-467 → UI feedback on failed search-468 → UI feedback on failed search-469 → UI feedback on failed search-470 → UI feedback on failed search-471
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // When I type "open nonexistent post xyz123" in Components.Chat.input
    await page.locator(testId(Chat.input)).fill('open nonexistent post xyz123');

    // And I click Components.Chat.sendButton
    await page.locator(testId(Chat.sendButton)).first().click();

    // Then Components.Chat.messages should contain a new message
    await expect(page.locator(testId(Chat.messages))).not.toBeEmpty();

  });

  test('Global navigation still works from blog page', async ({ page }) => {
    // Execution order: Global navigation still works from blog page-472 → Global navigation still works from blog page-473 → Global navigation still works from blog page-474 → Global navigation still works from blog page-475 → Global navigation still works from blog page-476 → Global navigation still works from blog page-477 → Global navigation still works from blog page-478
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // When I type "go to architecture" in Components.Chat.input
    await page.locator(testId(Chat.input)).fill('go to architecture');

    // And I click Components.Chat.sendButton
    await page.locator(testId(Chat.sendButton)).first().click();

    // Then Components.Chat.messages should contain a new message
    await expect(page.locator(testId(Chat.messages))).not.toBeEmpty();

    // And the current URL should be "/architecture"
    await expect(page).toHaveURL('/architecture');

  });

  test('Tool filtering shows correct counts in console', async ({ page }) => {
    // Execution order: Tool filtering shows correct counts in console-479 → Tool filtering shows correct counts in console-480 → Tool filtering shows correct counts in console-481 → Tool filtering shows correct counts in console-482 → Tool filtering shows correct counts in console-483 → Tool filtering shows correct counts in console-484 → Tool filtering shows correct counts in console-485 → Tool filtering shows correct counts in console-486
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // When I type "open your users" in Components.Chat.input
    await page.locator(testId(Chat.input)).fill('open your users');

    // And I click Components.Chat.sendButton
    await page.locator(testId(Chat.sendButton)).first().click();

    // Then the browser console should show "📦 [AI] Scoped tools: 1"
    await expect(page.locator('[data-testid="📦 [AI] Scoped tools: 1"]')).toHaveText('📦 [AI] Scoped tools: 1');

    // And the browser console should show "Global tools:"
    await expect(page.locator('[data-testid="Global tools:"]')).toHaveText('Global tools:');

    // And the browser console should show "Navigation tools:"
    await expect(page.locator('[data-testid="Navigation tools:"]')).toHaveText('Navigation tools:');

  });

  test('Match query with extra words to pattern', async ({ page }) => {
    // Execution order: Match query with extra words to pattern-487 → Match query with extra words to pattern-488 → Match query with extra words to pattern-489 → Match query with extra words to pattern-490 → Match query with extra words to pattern-491 → Match query with extra words to pattern-492
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // When I type "show me the documentation" in Components.Chat.input
    await page.locator(testId(Chat.input)).fill('show me the documentation');

    // And I click Components.Chat.sendButton
    await page.locator(testId(Chat.sendButton)).first().click();

    // Then Components.Chat.messages should contain a new message
    await expect(page.locator(testId(Chat.messages))).not.toBeEmpty();

  });

  test('Match query without extra words to pattern', async ({ page }) => {
    // Execution order: Match query without extra words to pattern-493 → Match query without extra words to pattern-494 → Match query without extra words to pattern-495 → Match query without extra words to pattern-496 → Match query without extra words to pattern-497 → Match query without extra words to pattern-498
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // When I type "reset" in Components.Chat.input
    await page.locator(testId(Chat.input)).fill('reset');

    // And I click Components.Chat.sendButton
    await page.locator(testId(Chat.sendButton)).first().click();

    // Then Components.Chat.messages should contain a new message
    await expect(page.locator(testId(Chat.messages))).not.toBeEmpty();

  });

  test('Blog search input is visible', async ({ page }) => {
    // Execution order: Blog search input is visible-499 → Blog search input is visible-500 → Blog search input is visible-501 → Blog search input is visible-502
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // Then Components.Blog.searchInput should be visible
    await expect(page.locator(testId(Blog.searchInput))).toBeVisible();

  });

  test('Blog category filter is visible', async ({ page }) => {
    // Execution order: Blog category filter is visible-503 → Blog category filter is visible-504 → Blog category filter is visible-505 → Blog category filter is visible-506
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // Then Components.Blog.categoryFilter should be visible
    await expect(page.locator(testId(Blog.categoryFilter))).toBeVisible();

  });

  test('Blog post cards are visible', async ({ page }) => {
    // Execution order: Blog post cards are visible-507 → Blog post cards are visible-508 → Blog post cards are visible-509 → Blog post cards are visible-510
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // Then Components.Blog.postCard should be visible
    await expect(page.locator(testId(Blog.postCard))).toBeVisible();

  });

  test('Can navigate back from blog', async ({ page }) => {
    // Execution order: Can navigate back from blog-511 → Can navigate back from blog-512 → Can navigate back from blog-513 → Can navigate back from blog-514 → Can navigate back from blog-515
    // Given I am on the blog page at "/blog"
    await page.goto('/blog');
    await initializeTestState(page);

    // And Components.Blog.container is visible
    await page.locator(testId(Blog.container)).first().waitFor({ state: 'visible' });

    // And Components.Chat.bubble is visible
    await page.locator(testId(Chat.bubble)).first().waitFor({ state: 'visible' });

    // When I click Components.Blog.backButton
    await page.locator(testId(Blog.backButton)).first().click();

    // Then the current URL should be "/"
    await expect(page).toHaveURL('/');

  });

});
