/**
 * CounterTools - Unit Tests
 * Auto-generated by Supernal Interface Test Generator
 * 
 * This file contains comprehensive tests for all @Tool decorated methods.
 * Tests include safety controls, AI execution permissions, and functionality.
 */

import { ToolRegistry } from '@supernal/interface';
import { CounterTools } from '../../src/tools/ExampleTools';

describe('CounterTools - Unit Tests', () => {
  let provider: CounterTools;

  beforeEach(() => {
    provider = new CounterTools();
    ToolRegistry.registerProvider('CounterTools', provider);
  });

  describe('Increment Legacy', () => {
    test('should execute successfully with valid parameters', async () => {
      const result = await ToolRegistry.executeForTesting('CounterTools.incrementLegacy', {});
      expect(result.success).toBe(true);
      expect(result.executionTime).toBeGreaterThan(0);
    });

    test('should be executable by AI (aiEnabled: true)', async () => {
      const result = await ToolRegistry.executeForAI('CounterTools.incrementLegacy', {});
      expect(result.success).toBe(true);
    });

    test('should have correct metadata', () => {
      const tools = ToolRegistry.getAllTools();
      const toolMeta = tools.get('CounterTools.incrementLegacy');
      
      expect(toolMeta).toBeDefined();
      expect(toolMeta!.name).toBe('Increment Legacy');
      expect(toolMeta!.aiEnabled).toBe(true);
      expect(toolMeta!.dangerLevel).toBe('safe');
      expect(toolMeta!.requiresApproval).toBe(false);
    });

  });

  describe('Decrement Legacy', () => {
    test('should execute successfully with valid parameters', async () => {
      const result = await ToolRegistry.executeForTesting('CounterTools.decrementLegacy', {});
      expect(result.success).toBe(true);
      expect(result.executionTime).toBeGreaterThan(0);
    });

    test('should reject AI execution (aiEnabled: false)', async () => {
      const result = await ToolRegistry.executeForAI('CounterTools.decrementLegacy', {});
      expect(result.success).toBe(false);
      expect(result.error).toContain('not AI-enabled');
    });

    test('should have correct metadata', () => {
      const tools = ToolRegistry.getAllTools();
      const toolMeta = tools.get('CounterTools.decrementLegacy');
      
      expect(toolMeta).toBeDefined();
      expect(toolMeta!.name).toBe('Decrement Legacy');
      expect(toolMeta!.aiEnabled).toBe(false);
      expect(toolMeta!.dangerLevel).toBe('safe');
      expect(toolMeta!.requiresApproval).toBe(false);
    });

  });

  describe('Reset Legacy', () => {
    test('should execute successfully with valid parameters', async () => {
      const result = await ToolRegistry.executeForTesting('CounterTools.resetLegacy', {});
      expect(result.success).toBe(true);
      expect(result.executionTime).toBeGreaterThan(0);
    });

    test('should be executable by AI (aiEnabled: true)', async () => {
      const result = await ToolRegistry.executeForAI('CounterTools.resetLegacy', {});
      expect(result.success).toBe(true);
    });

    test('should have correct metadata', () => {
      const tools = ToolRegistry.getAllTools();
      const toolMeta = tools.get('CounterTools.resetLegacy');
      
      expect(toolMeta).toBeDefined();
      expect(toolMeta!.name).toBe('Reset Legacy');
      expect(toolMeta!.aiEnabled).toBe(true);
      expect(toolMeta!.dangerLevel).toBe('safe');
      expect(toolMeta!.requiresApproval).toBe(false);
    });

  });

  describe('Safety Controls', () => {
    test('should enforce AI safety defaults', () => {
      const stats = ToolRegistry.getStats();
      
      // Verify dangerous tools require approval
      expect(stats.byDangerLevel.dangerous).toBeGreaterThanOrEqual(0);
      expect(stats.byDangerLevel.destructive).toBeGreaterThanOrEqual(0);
      
      // Verify approval requirements
      expect(stats.requiresApproval).toBeGreaterThanOrEqual(stats.byDangerLevel.dangerous + stats.byDangerLevel.destructive);
    });

  });

});
